[project]
name = "expense-tracker-mcp-remote"
version = "0.1.0"
description = "Expense Tracker MCP Server"
readme = "README.md"
requires-python = ">=3.13"

dependencies = [
    "fastmcp>=3.4.4",
    "aiosqlite>=0.21.0",
]
